"""Demo application built ON the pywebfw framework.

This package shows what a real project looks like: a thin shell that
configures the framework and adds its own features through `AppModule`
plugins — no framework code is modified.
"""
